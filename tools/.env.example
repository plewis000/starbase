# Pipeline Worker Configuration
# Copy to tools/.env and fill in values

# Shared secret — must match PIPELINE_SECRET in Vercel env vars
PIPELINE_SECRET=

# API base URL (production)
PIPELINE_API_URL=https://starbase-green.vercel.app

# Local repo path (absolute)
STARBASE_REPO_PATH=C:\Users\Parker\Desktop\Sisyphus\starbase

# Poll interval in milliseconds (default: 30 seconds)
POLL_INTERVAL_MS=30000

# Max time per job in milliseconds (default: 5 minutes)
JOB_TIMEOUT_MS=300000

# Claude Code command (default: claude)
# CLAUDE_CMD=claude
